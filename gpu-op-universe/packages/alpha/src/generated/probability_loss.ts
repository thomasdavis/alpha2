/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TensorOpRequest } from "../../../common/src/types";

/**
 * alpha.probability_loss.adaptive-softmax-loss
 * Adaptive softmax loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossAdaptiveSoftmaxLoss = defineStub<TensorOpRequest>("alpha.probability_loss.adaptive-softmax-loss");

/**
 * alpha.probability_loss.binary-cross-entropy
 * Binary cross entropy operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossBinaryCrossEntropy = defineStub<TensorOpRequest>("alpha.probability_loss.binary-cross-entropy");

/**
 * alpha.probability_loss.contrastive-loss
 * Contrastive loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossContrastiveLoss = defineStub<TensorOpRequest>("alpha.probability_loss.contrastive-loss");

/**
 * alpha.probability_loss.cosine-embedding-loss
 * Cosine embedding loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossCosineEmbeddingLoss = defineStub<TensorOpRequest>("alpha.probability_loss.cosine-embedding-loss");

/**
 * alpha.probability_loss.crf-backward
 * Crf backward operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossCrfBackward = defineStub<TensorOpRequest>("alpha.probability_loss.crf-backward");

/**
 * alpha.probability_loss.crf-forward
 * Crf forward operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossCrfForward = defineStub<TensorOpRequest>("alpha.probability_loss.crf-forward");

/**
 * alpha.probability_loss.cross-entropy
 * Cross entropy operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossCrossEntropy = defineStub<TensorOpRequest>("alpha.probability_loss.cross-entropy");

/**
 * alpha.probability_loss.ctc-loss
 * Ctc loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossCtcLoss = defineStub<TensorOpRequest>("alpha.probability_loss.ctc-loss");

/**
 * alpha.probability_loss.dpo-loss
 * Dpo loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossDpoLoss = defineStub<TensorOpRequest>("alpha.probability_loss.dpo-loss");

/**
 * alpha.probability_loss.entmax15
 * Entmax15 operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossEntmax15 = defineStub<TensorOpRequest>("alpha.probability_loss.entmax15");

/**
 * alpha.probability_loss.focal-loss
 * Focal loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossFocalLoss = defineStub<TensorOpRequest>("alpha.probability_loss.focal-loss");

/**
 * alpha.probability_loss.gumbel-softmax
 * Gumbel softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossGumbelSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.gumbel-softmax");

/**
 * alpha.probability_loss.halting-calibration-loss
 * Halting calibration loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossHaltingCalibrationLoss = defineStub<TensorOpRequest>("alpha.probability_loss.halting-calibration-loss");

/**
 * alpha.probability_loss.hellinger-distance
 * Hellinger distance operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossHellingerDistance = defineStub<TensorOpRequest>("alpha.probability_loss.hellinger-distance");

/**
 * alpha.probability_loss.hierarchical-softmax-loss
 * Hierarchical softmax loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossHierarchicalSoftmaxLoss = defineStub<TensorOpRequest>("alpha.probability_loss.hierarchical-softmax-loss");

/**
 * alpha.probability_loss.huber-loss
 * Huber loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossHuberLoss = defineStub<TensorOpRequest>("alpha.probability_loss.huber-loss");

/**
 * alpha.probability_loss.info-nce-loss
 * Info nce loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossInfoNceLoss = defineStub<TensorOpRequest>("alpha.probability_loss.info-nce-loss");

/**
 * alpha.probability_loss.ipo-loss
 * Ipo loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossIpoLoss = defineStub<TensorOpRequest>("alpha.probability_loss.ipo-loss");

/**
 * alpha.probability_loss.js-divergence
 * Js divergence operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossJsDivergence = defineStub<TensorOpRequest>("alpha.probability_loss.js-divergence");

/**
 * alpha.probability_loss.kl-divergence
 * Kl divergence operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossKlDivergence = defineStub<TensorOpRequest>("alpha.probability_loss.kl-divergence");

/**
 * alpha.probability_loss.kto-loss
 * Kto loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossKtoLoss = defineStub<TensorOpRequest>("alpha.probability_loss.kto-loss");

/**
 * alpha.probability_loss.label-smoothed-cross-entropy
 * Label smoothed cross entropy operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossLabelSmoothedCrossEntropy = defineStub<TensorOpRequest>("alpha.probability_loss.label-smoothed-cross-entropy");

/**
 * alpha.probability_loss.load-balance-loss
 * Load balance loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossLoadBalanceLoss = defineStub<TensorOpRequest>("alpha.probability_loss.load-balance-loss");

/**
 * alpha.probability_loss.log-softmax
 * Log softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossLogSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.log-softmax");

/**
 * alpha.probability_loss.mae-loss
 * Mae loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossMaeLoss = defineStub<TensorOpRequest>("alpha.probability_loss.mae-loss");

/**
 * alpha.probability_loss.margin-ranking-loss
 * Margin ranking loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossMarginRankingLoss = defineStub<TensorOpRequest>("alpha.probability_loss.margin-ranking-loss");

/**
 * alpha.probability_loss.masked-softmax
 * Masked softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossMaskedSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.masked-softmax");

/**
 * alpha.probability_loss.memory-reconstruction-loss
 * Memory reconstruction loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossMemoryReconstructionLoss = defineStub<TensorOpRequest>("alpha.probability_loss.memory-reconstruction-loss");

/**
 * alpha.probability_loss.mse-loss
 * Mse loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossMseLoss = defineStub<TensorOpRequest>("alpha.probability_loss.mse-loss");

/**
 * alpha.probability_loss.noise-contrastive-estimation
 * Noise contrastive estimation operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossNoiseContrastiveEstimation = defineStub<TensorOpRequest>("alpha.probability_loss.noise-contrastive-estimation");

/**
 * alpha.probability_loss.online-softmax
 * Online softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossOnlineSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.online-softmax");

/**
 * alpha.probability_loss.orpo-loss
 * Orpo loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossOrpoLoss = defineStub<TensorOpRequest>("alpha.probability_loss.orpo-loss");

/**
 * alpha.probability_loss.preference-loss
 * Preference loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossPreferenceLoss = defineStub<TensorOpRequest>("alpha.probability_loss.preference-loss");

/**
 * alpha.probability_loss.renyi-divergence
 * Renyi divergence operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossRenyiDivergence = defineStub<TensorOpRequest>("alpha.probability_loss.renyi-divergence");

/**
 * alpha.probability_loss.reverse-kl
 * Reverse kl operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossReverseKl = defineStub<TensorOpRequest>("alpha.probability_loss.reverse-kl");

/**
 * alpha.probability_loss.reward-model-loss
 * Reward model loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossRewardModelLoss = defineStub<TensorOpRequest>("alpha.probability_loss.reward-model-loss");

/**
 * alpha.probability_loss.route-consistency-loss
 * Route consistency loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossRouteConsistencyLoss = defineStub<TensorOpRequest>("alpha.probability_loss.route-consistency-loss");

/**
 * alpha.probability_loss.sampled-softmax-loss
 * Sampled softmax loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossSampledSoftmaxLoss = defineStub<TensorOpRequest>("alpha.probability_loss.sampled-softmax-loss");

/**
 * alpha.probability_loss.sequence-risk-loss
 * Sequence risk loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossSequenceRiskLoss = defineStub<TensorOpRequest>("alpha.probability_loss.sequence-risk-loss");

/**
 * alpha.probability_loss.smooth-l1-loss
 * Smooth l1 loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossSmoothL1Loss = defineStub<TensorOpRequest>("alpha.probability_loss.smooth-l1-loss");

/**
 * alpha.probability_loss.softmax
 * Softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.softmax");

/**
 * alpha.probability_loss.sparsemax
 * Sparsemax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossSparsemax = defineStub<TensorOpRequest>("alpha.probability_loss.sparsemax");

/**
 * alpha.probability_loss.temperature-softmax
 * Temperature softmax operation in the probability loss family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: differentiable.
 */
export const probabilityLossTemperatureSoftmax = defineStub<TensorOpRequest>("alpha.probability_loss.temperature-softmax");

/**
 * alpha.probability_loss.triplet-loss
 * Triplet loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossTripletLoss = defineStub<TensorOpRequest>("alpha.probability_loss.triplet-loss");

/**
 * alpha.probability_loss.wasserstein-loss
 * Wasserstein loss operation in the probability loss family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const probabilityLossWassersteinLoss = defineStub<TensorOpRequest>("alpha.probability_loss.wasserstein-loss");
