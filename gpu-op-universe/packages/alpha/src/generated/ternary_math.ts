/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { ScalarOpRequest } from "../../../common/src/types";

/**
 * alpha.ternary_math.add-div
 * Add div operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathAddDiv = defineStub<ScalarOpRequest>("alpha.ternary_math.add-div");

/**
 * alpha.ternary_math.add-mul
 * Add mul operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathAddMul = defineStub<ScalarOpRequest>("alpha.ternary_math.add-mul");

/**
 * alpha.ternary_math.clamp-tensor
 * Clamp tensor operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathClampTensor = defineStub<ScalarOpRequest>("alpha.ternary_math.clamp-tensor");

/**
 * alpha.ternary_math.complex-fma
 * Complex fma operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathComplexFma = defineStub<ScalarOpRequest>("alpha.ternary_math.complex-fma");

/**
 * alpha.ternary_math.conditional-swap
 * Conditional swap operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathConditionalSwap = defineStub<ScalarOpRequest>("alpha.ternary_math.conditional-swap");

/**
 * alpha.ternary_math.fma
 * Fma operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathFma = defineStub<ScalarOpRequest>("alpha.ternary_math.fma");

/**
 * alpha.ternary_math.lerp
 * Lerp operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathLerp = defineStub<ScalarOpRequest>("alpha.ternary_math.lerp");

/**
 * alpha.ternary_math.masked-fma
 * Masked fma operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathMaskedFma = defineStub<ScalarOpRequest>("alpha.ternary_math.masked-fma");

/**
 * alpha.ternary_math.median-of-three
 * Median of three operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathMedianOfThree = defineStub<ScalarOpRequest>("alpha.ternary_math.median-of-three");

/**
 * alpha.ternary_math.mul-add
 * Mul add operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathMulAdd = defineStub<ScalarOpRequest>("alpha.ternary_math.mul-add");

/**
 * alpha.ternary_math.saturating-fma
 * Saturating fma operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathSaturatingFma = defineStub<ScalarOpRequest>("alpha.ternary_math.saturating-fma");

/**
 * alpha.ternary_math.select-and-scatter
 * Select and scatter operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathSelectAndScatter = defineStub<ScalarOpRequest>("alpha.ternary_math.select-and-scatter");

/**
 * alpha.ternary_math.where-tensor
 * Where tensor operation in the ternary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const ternaryMathWhereTensor = defineStub<ScalarOpRequest>("alpha.ternary_math.where-tensor");
