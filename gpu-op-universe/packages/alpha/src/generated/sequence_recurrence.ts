/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { SequenceRequest } from "../../../common/src/types";

/**
 * alpha.sequence_recurrence.delta-rule
 * Delta rule operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceDeltaRule = defineStub<SequenceRequest>("alpha.sequence_recurrence.delta-rule");

/**
 * alpha.sequence_recurrence.diagonal-ssm
 * Diagonal ssm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceDiagonalSsm = defineStub<SequenceRequest>("alpha.sequence_recurrence.diagonal-ssm");

/**
 * alpha.sequence_recurrence.equilibrium-solve
 * Equilibrium solve operation in the sequence recurrence family.
 * Status: speculative; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceEquilibriumSolve = defineStub<SequenceRequest>("alpha.sequence_recurrence.equilibrium-solve");

/**
 * alpha.sequence_recurrence.event-driven-mechanism
 * Event driven mechanism operation in the sequence recurrence family.
 * Status: research; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceEventDrivenMechanism = defineStub<SequenceRequest>("alpha.sequence_recurrence.event-driven-mechanism");

/**
 * alpha.sequence_recurrence.fast-weight-update
 * Fast weight update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceFastWeightUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.fast-weight-update");

/**
 * alpha.sequence_recurrence.fixed-wave-mechanism
 * Fixed wave mechanism operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceFixedWaveMechanism = defineStub<SequenceRequest>("alpha.sequence_recurrence.fixed-wave-mechanism");

/**
 * alpha.sequence_recurrence.gated-delta-rule
 * Gated delta rule operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceGatedDeltaRule = defineStub<SequenceRequest>("alpha.sequence_recurrence.gated-delta-rule");

/**
 * alpha.sequence_recurrence.gru
 * Gru operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceGru = defineStub<SequenceRequest>("alpha.sequence_recurrence.gru");

/**
 * alpha.sequence_recurrence.h3-recurrence
 * H3 recurrence operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceH3Recurrence = defineStub<SequenceRequest>("alpha.sequence_recurrence.h3-recurrence");

/**
 * alpha.sequence_recurrence.hebbian-update
 * Hebbian update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceHebbianUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.hebbian-update");

/**
 * alpha.sequence_recurrence.hyena-recurrence
 * Hyena recurrence operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceHyenaRecurrence = defineStub<SequenceRequest>("alpha.sequence_recurrence.hyena-recurrence");

/**
 * alpha.sequence_recurrence.implicit-state-update
 * Implicit state update operation in the sequence recurrence family.
 * Status: speculative; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceImplicitStateUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.implicit-state-update");

/**
 * alpha.sequence_recurrence.ind-rnn
 * Ind rnn operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceIndRnn = defineStub<SequenceRequest>("alpha.sequence_recurrence.ind-rnn");

/**
 * alpha.sequence_recurrence.linear-attention-recurrence
 * Linear attention recurrence operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceLinearAttentionRecurrence = defineStub<SequenceRequest>("alpha.sequence_recurrence.linear-attention-recurrence");

/**
 * alpha.sequence_recurrence.liquid-time-constant
 * Liquid time constant operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceLiquidTimeConstant = defineStub<SequenceRequest>("alpha.sequence_recurrence.liquid-time-constant");

/**
 * alpha.sequence_recurrence.lstm
 * Lstm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceLstm = defineStub<SequenceRequest>("alpha.sequence_recurrence.lstm");

/**
 * alpha.sequence_recurrence.mamba-selective-scan
 * Mamba selective scan operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceMambaSelectiveScan = defineStub<SequenceRequest>("alpha.sequence_recurrence.mamba-selective-scan");

/**
 * alpha.sequence_recurrence.minimal-gru
 * Minimal gru operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceMinimalGru = defineStub<SequenceRequest>("alpha.sequence_recurrence.minimal-gru");

/**
 * alpha.sequence_recurrence.m-lstm
 * M lstm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceMLstm = defineStub<SequenceRequest>("alpha.sequence_recurrence.m-lstm");

/**
 * alpha.sequence_recurrence.modern-hopfield-update
 * Modern hopfield update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceModernHopfieldUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.modern-hopfield-update");

/**
 * alpha.sequence_recurrence.neural-ode-step
 * Neural ode step operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceNeuralOdeStep = defineStub<SequenceRequest>("alpha.sequence_recurrence.neural-ode-step");

/**
 * alpha.sequence_recurrence.oja-update
 * Oja update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceOjaUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.oja-update");

/**
 * alpha.sequence_recurrence.orthogonal-rnn
 * Orthogonal rnn operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceOrthogonalRnn = defineStub<SequenceRequest>("alpha.sequence_recurrence.orthogonal-rnn");

/**
 * alpha.sequence_recurrence.peephole-lstm
 * Peephole lstm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrencePeepholeLstm = defineStub<SequenceRequest>("alpha.sequence_recurrence.peephole-lstm");

/**
 * alpha.sequence_recurrence.qrnn
 * Qrnn operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceQrnn = defineStub<SequenceRequest>("alpha.sequence_recurrence.qrnn");

/**
 * alpha.sequence_recurrence.ret-net-recurrence
 * Ret net recurrence operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceRetNetRecurrence = defineStub<SequenceRequest>("alpha.sequence_recurrence.ret-net-recurrence");

/**
 * alpha.sequence_recurrence.rwkv7-state-update
 * Rwkv7 state update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceRwkv7StateUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.rwkv7-state-update");

/**
 * alpha.sequence_recurrence.rwkv-wkv
 * Rwkv wkv operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceRwkvWkv = defineStub<SequenceRequest>("alpha.sequence_recurrence.rwkv-wkv");

/**
 * alpha.sequence_recurrence.s4-kernel
 * S4 kernel operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceS4Kernel = defineStub<SequenceRequest>("alpha.sequence_recurrence.s4-kernel");

/**
 * alpha.sequence_recurrence.selective-state-update
 * Selective state update operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceSelectiveStateUpdate = defineStub<SequenceRequest>("alpha.sequence_recurrence.selective-state-update");

/**
 * alpha.sequence_recurrence.simple-rnn
 * Simple rnn operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceSimpleRnn = defineStub<SequenceRequest>("alpha.sequence_recurrence.simple-rnn");

/**
 * alpha.sequence_recurrence.s-lstm
 * S lstm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceSLstm = defineStub<SequenceRequest>("alpha.sequence_recurrence.s-lstm");

/**
 * alpha.sequence_recurrence.sru
 * Sru operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceSru = defineStub<SequenceRequest>("alpha.sequence_recurrence.sru");

/**
 * alpha.sequence_recurrence.state-compress
 * State compress operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStateCompress = defineStub<SequenceRequest>("alpha.sequence_recurrence.state-compress");

/**
 * alpha.sequence_recurrence.state-expand
 * State expand operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStateExpand = defineStub<SequenceRequest>("alpha.sequence_recurrence.state-expand");

/**
 * alpha.sequence_recurrence.state-reset
 * State reset operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStateReset = defineStub<SequenceRequest>("alpha.sequence_recurrence.state-reset");

/**
 * alpha.sequence_recurrence.state-restore
 * State restore operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStateRestore = defineStub<SequenceRequest>("alpha.sequence_recurrence.state-restore");

/**
 * alpha.sequence_recurrence.state-snapshot
 * State snapshot operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStateSnapshot = defineStub<SequenceRequest>("alpha.sequence_recurrence.state-snapshot");

/**
 * alpha.sequence_recurrence.structured-ssm
 * Structured ssm operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceStructuredSsm = defineStub<SequenceRequest>("alpha.sequence_recurrence.structured-ssm");

/**
 * alpha.sequence_recurrence.unitary-rnn
 * Unitary rnn operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceUnitaryRnn = defineStub<SequenceRequest>("alpha.sequence_recurrence.unitary-rnn");

/**
 * alpha.sequence_recurrence.x-lstm-block
 * X lstm block operation in the sequence recurrence family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable-or-implicit.
 */
export const sequenceRecurrenceXLstmBlock = defineStub<SequenceRequest>("alpha.sequence_recurrence.x-lstm-block");
