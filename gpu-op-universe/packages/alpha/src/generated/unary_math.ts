/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { ScalarOpRequest } from "../../../common/src/types";

/**
 * alpha.unary_math.abs
 * Abs operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAbs = defineStub<ScalarOpRequest>("alpha.unary_math.abs");

/**
 * alpha.unary_math.acos
 * Acos operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAcos = defineStub<ScalarOpRequest>("alpha.unary_math.acos");

/**
 * alpha.unary_math.acosh
 * Acosh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAcosh = defineStub<ScalarOpRequest>("alpha.unary_math.acosh");

/**
 * alpha.unary_math.angle
 * Angle operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAngle = defineStub<ScalarOpRequest>("alpha.unary_math.angle");

/**
 * alpha.unary_math.asin
 * Asin operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAsin = defineStub<ScalarOpRequest>("alpha.unary_math.asin");

/**
 * alpha.unary_math.asinh
 * Asinh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAsinh = defineStub<ScalarOpRequest>("alpha.unary_math.asinh");

/**
 * alpha.unary_math.atan
 * Atan operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAtan = defineStub<ScalarOpRequest>("alpha.unary_math.atan");

/**
 * alpha.unary_math.atanh
 * Atanh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathAtanh = defineStub<ScalarOpRequest>("alpha.unary_math.atanh");

/**
 * alpha.unary_math.bessel-i0
 * Bessel i0 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathBesselI0 = defineStub<ScalarOpRequest>("alpha.unary_math.bessel-i0");

/**
 * alpha.unary_math.bessel-i1
 * Bessel i1 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathBesselI1 = defineStub<ScalarOpRequest>("alpha.unary_math.bessel-i1");

/**
 * alpha.unary_math.bessel-j0
 * Bessel j0 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathBesselJ0 = defineStub<ScalarOpRequest>("alpha.unary_math.bessel-j0");

/**
 * alpha.unary_math.bessel-j1
 * Bessel j1 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathBesselJ1 = defineStub<ScalarOpRequest>("alpha.unary_math.bessel-j1");

/**
 * alpha.unary_math.bitwise-not
 * Bitwise not operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathBitwiseNot = defineStub<ScalarOpRequest>("alpha.unary_math.bitwise-not");

/**
 * alpha.unary_math.cbrt
 * Cbrt operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCbrt = defineStub<ScalarOpRequest>("alpha.unary_math.cbrt");

/**
 * alpha.unary_math.ceil
 * Ceil operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCeil = defineStub<ScalarOpRequest>("alpha.unary_math.ceil");

/**
 * alpha.unary_math.celu
 * Celu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCelu = defineStub<ScalarOpRequest>("alpha.unary_math.celu");

/**
 * alpha.unary_math.clamp
 * Clamp operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathClamp = defineStub<ScalarOpRequest>("alpha.unary_math.clamp");

/**
 * alpha.unary_math.clip-by-value
 * Clip by value operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathClipByValue = defineStub<ScalarOpRequest>("alpha.unary_math.clip-by-value");

/**
 * alpha.unary_math.complex-abs
 * Complex abs operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathComplexAbs = defineStub<ScalarOpRequest>("alpha.unary_math.complex-abs");

/**
 * alpha.unary_math.conj
 * Conj operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathConj = defineStub<ScalarOpRequest>("alpha.unary_math.conj");

/**
 * alpha.unary_math.cos
 * Cos operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCos = defineStub<ScalarOpRequest>("alpha.unary_math.cos");

/**
 * alpha.unary_math.cosh
 * Cosh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCosh = defineStub<ScalarOpRequest>("alpha.unary_math.cosh");

/**
 * alpha.unary_math.count-leading-zeros
 * Count leading zeros operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCountLeadingZeros = defineStub<ScalarOpRequest>("alpha.unary_math.count-leading-zeros");

/**
 * alpha.unary_math.count-trailing-zeros
 * Count trailing zeros operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCountTrailingZeros = defineStub<ScalarOpRequest>("alpha.unary_math.count-trailing-zeros");

/**
 * alpha.unary_math.cube
 * Cube operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathCube = defineStub<ScalarOpRequest>("alpha.unary_math.cube");

/**
 * alpha.unary_math.digamma
 * Digamma operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathDigamma = defineStub<ScalarOpRequest>("alpha.unary_math.digamma");

/**
 * alpha.unary_math.elu
 * Elu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathElu = defineStub<ScalarOpRequest>("alpha.unary_math.elu");

/**
 * alpha.unary_math.erf
 * Erf operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathErf = defineStub<ScalarOpRequest>("alpha.unary_math.erf");

/**
 * alpha.unary_math.erfc
 * Erfc operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathErfc = defineStub<ScalarOpRequest>("alpha.unary_math.erfc");

/**
 * alpha.unary_math.erfinv
 * Erfinv operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathErfinv = defineStub<ScalarOpRequest>("alpha.unary_math.erfinv");

/**
 * alpha.unary_math.exp
 * Exp operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathExp = defineStub<ScalarOpRequest>("alpha.unary_math.exp");

/**
 * alpha.unary_math.exp10
 * Exp10 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathExp10 = defineStub<ScalarOpRequest>("alpha.unary_math.exp10");

/**
 * alpha.unary_math.exp2
 * Exp2 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathExp2 = defineStub<ScalarOpRequest>("alpha.unary_math.exp2");

/**
 * alpha.unary_math.expm1
 * Expm1 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathExpm1 = defineStub<ScalarOpRequest>("alpha.unary_math.expm1");

/**
 * alpha.unary_math.find-first-set
 * Find first set operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathFindFirstSet = defineStub<ScalarOpRequest>("alpha.unary_math.find-first-set");

/**
 * alpha.unary_math.floor
 * Floor operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathFloor = defineStub<ScalarOpRequest>("alpha.unary_math.floor");

/**
 * alpha.unary_math.frac
 * Frac operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathFrac = defineStub<ScalarOpRequest>("alpha.unary_math.frac");

/**
 * alpha.unary_math.gamma
 * Gamma operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathGamma = defineStub<ScalarOpRequest>("alpha.unary_math.gamma");

/**
 * alpha.unary_math.gelu-exact
 * Gelu exact operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathGeluExact = defineStub<ScalarOpRequest>("alpha.unary_math.gelu-exact");

/**
 * alpha.unary_math.gelu-quick
 * Gelu quick operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathGeluQuick = defineStub<ScalarOpRequest>("alpha.unary_math.gelu-quick");

/**
 * alpha.unary_math.gelu-tanh
 * Gelu tanh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathGeluTanh = defineStub<ScalarOpRequest>("alpha.unary_math.gelu-tanh");

/**
 * alpha.unary_math.hard-sigmoid
 * Hard sigmoid operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathHardSigmoid = defineStub<ScalarOpRequest>("alpha.unary_math.hard-sigmoid");

/**
 * alpha.unary_math.hard-swish
 * Hard swish operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathHardSwish = defineStub<ScalarOpRequest>("alpha.unary_math.hard-swish");

/**
 * alpha.unary_math.imag
 * Imag operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathImag = defineStub<ScalarOpRequest>("alpha.unary_math.imag");

/**
 * alpha.unary_math.is-finite
 * Is finite operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathIsFinite = defineStub<ScalarOpRequest>("alpha.unary_math.is-finite");

/**
 * alpha.unary_math.is-inf
 * Is inf operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathIsInf = defineStub<ScalarOpRequest>("alpha.unary_math.is-inf");

/**
 * alpha.unary_math.is-nan
 * Is nan operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathIsNan = defineStub<ScalarOpRequest>("alpha.unary_math.is-nan");

/**
 * alpha.unary_math.is-normal
 * Is normal operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathIsNormal = defineStub<ScalarOpRequest>("alpha.unary_math.is-normal");

/**
 * alpha.unary_math.leaky-relu
 * Leaky relu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLeakyRelu = defineStub<ScalarOpRequest>("alpha.unary_math.leaky-relu");

/**
 * alpha.unary_math.lgamma
 * Lgamma operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLgamma = defineStub<ScalarOpRequest>("alpha.unary_math.lgamma");

/**
 * alpha.unary_math.log
 * Log operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLog = defineStub<ScalarOpRequest>("alpha.unary_math.log");

/**
 * alpha.unary_math.log10
 * Log10 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLog10 = defineStub<ScalarOpRequest>("alpha.unary_math.log10");

/**
 * alpha.unary_math.log1p
 * Log1p operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLog1p = defineStub<ScalarOpRequest>("alpha.unary_math.log1p");

/**
 * alpha.unary_math.log2
 * Log2 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLog2 = defineStub<ScalarOpRequest>("alpha.unary_math.log2");

/**
 * alpha.unary_math.logical-not
 * Logical not operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLogicalNot = defineStub<ScalarOpRequest>("alpha.unary_math.logical-not");

/**
 * alpha.unary_math.log-sigmoid
 * Log sigmoid operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathLogSigmoid = defineStub<ScalarOpRequest>("alpha.unary_math.log-sigmoid");

/**
 * alpha.unary_math.mish
 * Mish operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathMish = defineStub<ScalarOpRequest>("alpha.unary_math.mish");

/**
 * alpha.unary_math.neg
 * Neg operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathNeg = defineStub<ScalarOpRequest>("alpha.unary_math.neg");

/**
 * alpha.unary_math.population-count
 * Population count operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathPopulationCount = defineStub<ScalarOpRequest>("alpha.unary_math.population-count");

/**
 * alpha.unary_math.prelu
 * Prelu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathPrelu = defineStub<ScalarOpRequest>("alpha.unary_math.prelu");

/**
 * alpha.unary_math.real
 * Real operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathReal = defineStub<ScalarOpRequest>("alpha.unary_math.real");

/**
 * alpha.unary_math.reciprocal
 * Reciprocal operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathReciprocal = defineStub<ScalarOpRequest>("alpha.unary_math.reciprocal");

/**
 * alpha.unary_math.relu
 * Relu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathRelu = defineStub<ScalarOpRequest>("alpha.unary_math.relu");

/**
 * alpha.unary_math.relu6
 * Relu6 operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathRelu6 = defineStub<ScalarOpRequest>("alpha.unary_math.relu6");

/**
 * alpha.unary_math.reverse-bits
 * Reverse bits operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathReverseBits = defineStub<ScalarOpRequest>("alpha.unary_math.reverse-bits");

/**
 * alpha.unary_math.round
 * Round operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathRound = defineStub<ScalarOpRequest>("alpha.unary_math.round");

/**
 * alpha.unary_math.round-even
 * Round even operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathRoundEven = defineStub<ScalarOpRequest>("alpha.unary_math.round-even");

/**
 * alpha.unary_math.rsqrt
 * Rsqrt operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathRsqrt = defineStub<ScalarOpRequest>("alpha.unary_math.rsqrt");

/**
 * alpha.unary_math.selu
 * Selu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSelu = defineStub<ScalarOpRequest>("alpha.unary_math.selu");

/**
 * alpha.unary_math.sigmoid
 * Sigmoid operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSigmoid = defineStub<ScalarOpRequest>("alpha.unary_math.sigmoid");

/**
 * alpha.unary_math.sign
 * Sign operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSign = defineStub<ScalarOpRequest>("alpha.unary_math.sign");

/**
 * alpha.unary_math.silu
 * Silu operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSilu = defineStub<ScalarOpRequest>("alpha.unary_math.silu");

/**
 * alpha.unary_math.sin
 * Sin operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSin = defineStub<ScalarOpRequest>("alpha.unary_math.sin");

/**
 * alpha.unary_math.sinc
 * Sinc operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSinc = defineStub<ScalarOpRequest>("alpha.unary_math.sinc");

/**
 * alpha.unary_math.sinh
 * Sinh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSinh = defineStub<ScalarOpRequest>("alpha.unary_math.sinh");

/**
 * alpha.unary_math.sinhc
 * Sinhc operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSinhc = defineStub<ScalarOpRequest>("alpha.unary_math.sinhc");

/**
 * alpha.unary_math.softplus
 * Softplus operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSoftplus = defineStub<ScalarOpRequest>("alpha.unary_math.softplus");

/**
 * alpha.unary_math.softsign
 * Softsign operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSoftsign = defineStub<ScalarOpRequest>("alpha.unary_math.softsign");

/**
 * alpha.unary_math.sqrt
 * Sqrt operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSqrt = defineStub<ScalarOpRequest>("alpha.unary_math.sqrt");

/**
 * alpha.unary_math.square
 * Square operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathSquare = defineStub<ScalarOpRequest>("alpha.unary_math.square");

/**
 * alpha.unary_math.tan
 * Tan operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathTan = defineStub<ScalarOpRequest>("alpha.unary_math.tan");

/**
 * alpha.unary_math.tanh
 * Tanh operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathTanh = defineStub<ScalarOpRequest>("alpha.unary_math.tanh");

/**
 * alpha.unary_math.threshold
 * Threshold operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathThreshold = defineStub<ScalarOpRequest>("alpha.unary_math.threshold");

/**
 * alpha.unary_math.trigamma
 * Trigamma operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathTrigamma = defineStub<ScalarOpRequest>("alpha.unary_math.trigamma");

/**
 * alpha.unary_math.trunc
 * Trunc operation in the unary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const unaryMathTrunc = defineStub<ScalarOpRequest>("alpha.unary_math.trunc");
