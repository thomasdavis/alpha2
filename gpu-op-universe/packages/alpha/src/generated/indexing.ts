/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TensorOpRequest } from "../../../common/src/types";

/**
 * alpha.indexing.argsort
 * Argsort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingArgsort = defineStub<TensorOpRequest>("alpha.indexing.argsort");

/**
 * alpha.indexing.argwhere
 * Argwhere operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingArgwhere = defineStub<TensorOpRequest>("alpha.indexing.argwhere");

/**
 * alpha.indexing.bitonic-sort
 * Bitonic sort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingBitonicSort = defineStub<TensorOpRequest>("alpha.indexing.bitonic-sort");

/**
 * alpha.indexing.bottom-k
 * Bottom k operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingBottomK = defineStub<TensorOpRequest>("alpha.indexing.bottom-k");

/**
 * alpha.indexing.bucketize
 * Bucketize operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingBucketize = defineStub<TensorOpRequest>("alpha.indexing.bucketize");

/**
 * alpha.indexing.dynamic-slice
 * Dynamic slice operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingDynamicSlice = defineStub<TensorOpRequest>("alpha.indexing.dynamic-slice");

/**
 * alpha.indexing.dynamic-update-slice
 * Dynamic update slice operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingDynamicUpdateSlice = defineStub<TensorOpRequest>("alpha.indexing.dynamic-update-slice");

/**
 * alpha.indexing.embedding-bag
 * Embedding bag operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingEmbeddingBag = defineStub<TensorOpRequest>("alpha.indexing.embedding-bag");

/**
 * alpha.indexing.embedding-lookup
 * Embedding lookup operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingEmbeddingLookup = defineStub<TensorOpRequest>("alpha.indexing.embedding-lookup");

/**
 * alpha.indexing.gather
 * Gather operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingGather = defineStub<TensorOpRequest>("alpha.indexing.gather");

/**
 * alpha.indexing.gather-nd
 * Gather nd operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingGatherNd = defineStub<TensorOpRequest>("alpha.indexing.gather-nd");

/**
 * alpha.indexing.index-add
 * Index add operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingIndexAdd = defineStub<TensorOpRequest>("alpha.indexing.index-add");

/**
 * alpha.indexing.index-put
 * Index put operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingIndexPut = defineStub<TensorOpRequest>("alpha.indexing.index-put");

/**
 * alpha.indexing.index-select
 * Index select operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingIndexSelect = defineStub<TensorOpRequest>("alpha.indexing.index-select");

/**
 * alpha.indexing.kth-value
 * Kth value operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingKthValue = defineStub<TensorOpRequest>("alpha.indexing.kth-value");

/**
 * alpha.indexing.lex-sort
 * Lex sort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingLexSort = defineStub<TensorOpRequest>("alpha.indexing.lex-sort");

/**
 * alpha.indexing.masked-fill
 * Masked fill operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingMaskedFill = defineStub<TensorOpRequest>("alpha.indexing.masked-fill");

/**
 * alpha.indexing.masked-scatter
 * Masked scatter operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingMaskedScatter = defineStub<TensorOpRequest>("alpha.indexing.masked-scatter");

/**
 * alpha.indexing.masked-select
 * Masked select operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingMaskedSelect = defineStub<TensorOpRequest>("alpha.indexing.masked-select");

/**
 * alpha.indexing.merge-sorted
 * Merge sorted operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingMergeSorted = defineStub<TensorOpRequest>("alpha.indexing.merge-sorted");

/**
 * alpha.indexing.narrow
 * Narrow operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingNarrow = defineStub<TensorOpRequest>("alpha.indexing.narrow");

/**
 * alpha.indexing.nonzero
 * Nonzero operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingNonzero = defineStub<TensorOpRequest>("alpha.indexing.nonzero");

/**
 * alpha.indexing.nth-element
 * Nth element operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingNthElement = defineStub<TensorOpRequest>("alpha.indexing.nth-element");

/**
 * alpha.indexing.one-hot
 * One hot operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingOneHot = defineStub<TensorOpRequest>("alpha.indexing.one-hot");

/**
 * alpha.indexing.partition
 * Partition operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingPartition = defineStub<TensorOpRequest>("alpha.indexing.partition");

/**
 * alpha.indexing.put
 * Put operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingPut = defineStub<TensorOpRequest>("alpha.indexing.put");

/**
 * alpha.indexing.put-along-dim
 * Put along dim operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingPutAlongDim = defineStub<TensorOpRequest>("alpha.indexing.put-along-dim");

/**
 * alpha.indexing.radix-sort
 * Radix sort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingRadixSort = defineStub<TensorOpRequest>("alpha.indexing.radix-sort");

/**
 * alpha.indexing.sample-without-replacement
 * Sample without replacement operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSampleWithoutReplacement = defineStub<TensorOpRequest>("alpha.indexing.sample-without-replacement");

/**
 * alpha.indexing.scatter
 * Scatter operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatter = defineStub<TensorOpRequest>("alpha.indexing.scatter");

/**
 * alpha.indexing.scatter-add
 * Scatter add operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterAdd = defineStub<TensorOpRequest>("alpha.indexing.scatter-add");

/**
 * alpha.indexing.scatter-max
 * Scatter max operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterMax = defineStub<TensorOpRequest>("alpha.indexing.scatter-max");

/**
 * alpha.indexing.scatter-mean
 * Scatter mean operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterMean = defineStub<TensorOpRequest>("alpha.indexing.scatter-mean");

/**
 * alpha.indexing.scatter-min
 * Scatter min operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterMin = defineStub<TensorOpRequest>("alpha.indexing.scatter-min");

/**
 * alpha.indexing.scatter-mul
 * Scatter mul operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterMul = defineStub<TensorOpRequest>("alpha.indexing.scatter-mul");

/**
 * alpha.indexing.scatter-reduce
 * Scatter reduce operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingScatterReduce = defineStub<TensorOpRequest>("alpha.indexing.scatter-reduce");

/**
 * alpha.indexing.search-sorted
 * Search sorted operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSearchSorted = defineStub<TensorOpRequest>("alpha.indexing.search-sorted");

/**
 * alpha.indexing.segment-ids
 * Segment ids operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSegmentIds = defineStub<TensorOpRequest>("alpha.indexing.segment-ids");

/**
 * alpha.indexing.select
 * Select operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSelect = defineStub<TensorOpRequest>("alpha.indexing.select");

/**
 * alpha.indexing.slice
 * Slice operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSlice = defineStub<TensorOpRequest>("alpha.indexing.slice");

/**
 * alpha.indexing.slice-update
 * Slice update operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSliceUpdate = defineStub<TensorOpRequest>("alpha.indexing.slice-update");

/**
 * alpha.indexing.sort
 * Sort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingSort = defineStub<TensorOpRequest>("alpha.indexing.sort");

/**
 * alpha.indexing.stable-sort
 * Stable sort operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingStableSort = defineStub<TensorOpRequest>("alpha.indexing.stable-sort");

/**
 * alpha.indexing.take
 * Take operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingTake = defineStub<TensorOpRequest>("alpha.indexing.take");

/**
 * alpha.indexing.take-along-dim
 * Take along dim operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingTakeAlongDim = defineStub<TensorOpRequest>("alpha.indexing.take-along-dim");

/**
 * alpha.indexing.top-k
 * Top k operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingTopK = defineStub<TensorOpRequest>("alpha.indexing.top-k");

/**
 * alpha.indexing.unique
 * Unique operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingUnique = defineStub<TensorOpRequest>("alpha.indexing.unique");

/**
 * alpha.indexing.unique-consecutive
 * Unique consecutive operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingUniqueConsecutive = defineStub<TensorOpRequest>("alpha.indexing.unique-consecutive");

/**
 * alpha.indexing.where
 * Where operation in the indexing family.
 * Status: standard; target: architecture-agnostic; differentiability: piecewise-or-custom.
 */
export const indexingWhere = defineStub<TensorOpRequest>("alpha.indexing.where");
