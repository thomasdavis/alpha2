/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TensorOpRequest } from "../../../common/src/types";

/**
 * alpha.convolution.block-sparse-conv
 * Block sparse conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionBlockSparseConv = defineStub<TensorOpRequest>("alpha.convolution.block-sparse-conv");

/**
 * alpha.convolution.causal-conv
 * Causal conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionCausalConv = defineStub<TensorOpRequest>("alpha.convolution.causal-conv");

/**
 * alpha.convolution.conv1d
 * Conv1d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConv1d = defineStub<TensorOpRequest>("alpha.convolution.conv1d");

/**
 * alpha.convolution.conv2d
 * Conv2d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConv2d = defineStub<TensorOpRequest>("alpha.convolution.conv2d");

/**
 * alpha.convolution.conv3d
 * Conv3d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConv3d = defineStub<TensorOpRequest>("alpha.convolution.conv3d");

/**
 * alpha.convolution.conv-backward-bias
 * Conv backward bias operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvBackwardBias = defineStub<TensorOpRequest>("alpha.convolution.conv-backward-bias");

/**
 * alpha.convolution.conv-backward-input
 * Conv backward input operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvBackwardInput = defineStub<TensorOpRequest>("alpha.convolution.conv-backward-input");

/**
 * alpha.convolution.conv-backward-weight
 * Conv backward weight operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvBackwardWeight = defineStub<TensorOpRequest>("alpha.convolution.conv-backward-weight");

/**
 * alpha.convolution.conv-transpose1d
 * Conv transpose1d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvTranspose1d = defineStub<TensorOpRequest>("alpha.convolution.conv-transpose1d");

/**
 * alpha.convolution.conv-transpose2d
 * Conv transpose2d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvTranspose2d = defineStub<TensorOpRequest>("alpha.convolution.conv-transpose2d");

/**
 * alpha.convolution.conv-transpose3d
 * Conv transpose3d operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionConvTranspose3d = defineStub<TensorOpRequest>("alpha.convolution.conv-transpose3d");

/**
 * alpha.convolution.deformable-conv
 * Deformable conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionDeformableConv = defineStub<TensorOpRequest>("alpha.convolution.deformable-conv");

/**
 * alpha.convolution.depthwise-conv
 * Depthwise conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionDepthwiseConv = defineStub<TensorOpRequest>("alpha.convolution.depthwise-conv");

/**
 * alpha.convolution.dilated-conv
 * Dilated conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionDilatedConv = defineStub<TensorOpRequest>("alpha.convolution.dilated-conv");

/**
 * alpha.convolution.direct-conv
 * Direct conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionDirectConv = defineStub<TensorOpRequest>("alpha.convolution.direct-conv");

/**
 * alpha.convolution.dynamic-conv
 * Dynamic conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionDynamicConv = defineStub<TensorOpRequest>("alpha.convolution.dynamic-conv");

/**
 * alpha.convolution.fft-conv
 * Fft conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionFftConv = defineStub<TensorOpRequest>("alpha.convolution.fft-conv");

/**
 * alpha.convolution.grouped-conv
 * Grouped conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionGroupedConv = defineStub<TensorOpRequest>("alpha.convolution.grouped-conv");

/**
 * alpha.convolution.hyena-long-conv
 * Hyena long conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionHyenaLongConv = defineStub<TensorOpRequest>("alpha.convolution.hyena-long-conv");

/**
 * alpha.convolution.implicit-gemm-conv
 * Implicit gemm conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionImplicitGemmConv = defineStub<TensorOpRequest>("alpha.convolution.implicit-gemm-conv");

/**
 * alpha.convolution.masked-conv
 * Masked conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionMaskedConv = defineStub<TensorOpRequest>("alpha.convolution.masked-conv");

/**
 * alpha.convolution.pointwise-conv
 * Pointwise conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionPointwiseConv = defineStub<TensorOpRequest>("alpha.convolution.pointwise-conv");

/**
 * alpha.convolution.separable-conv
 * Separable conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionSeparableConv = defineStub<TensorOpRequest>("alpha.convolution.separable-conv");

/**
 * alpha.convolution.state-space-conv
 * State space conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionStateSpaceConv = defineStub<TensorOpRequest>("alpha.convolution.state-space-conv");

/**
 * alpha.convolution.toeplitz-conv
 * Toeplitz conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionToeplitzConv = defineStub<TensorOpRequest>("alpha.convolution.toeplitz-conv");

/**
 * alpha.convolution.winograd-conv
 * Winograd conv operation in the convolution family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const convolutionWinogradConv = defineStub<TensorOpRequest>("alpha.convolution.winograd-conv");
