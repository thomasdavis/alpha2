/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TransformOpRequest } from "../../../common/src/types";

/**
 * alpha.shape_layout.alias
 * Alias operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutAlias = defineStub<TransformOpRequest>("alpha.shape_layout.alias");

/**
 * alpha.shape_layout.as-strided
 * As strided operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutAsStrided = defineStub<TransformOpRequest>("alpha.shape_layout.as-strided");

/**
 * alpha.shape_layout.bitcast
 * Bitcast operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutBitcast = defineStub<TransformOpRequest>("alpha.shape_layout.bitcast");

/**
 * alpha.shape_layout.block
 * Block operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutBlock = defineStub<TransformOpRequest>("alpha.shape_layout.block");

/**
 * alpha.shape_layout.broadcast-in-dim
 * Broadcast in dim operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutBroadcastInDim = defineStub<TransformOpRequest>("alpha.shape_layout.broadcast-in-dim");

/**
 * alpha.shape_layout.broadcast-to
 * Broadcast to operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutBroadcastTo = defineStub<TransformOpRequest>("alpha.shape_layout.broadcast-to");

/**
 * alpha.shape_layout.clone
 * Clone operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutClone = defineStub<TransformOpRequest>("alpha.shape_layout.clone");

/**
 * alpha.shape_layout.contiguous
 * Contiguous operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutContiguous = defineStub<TransformOpRequest>("alpha.shape_layout.contiguous");

/**
 * alpha.shape_layout.copy
 * Copy operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutCopy = defineStub<TransformOpRequest>("alpha.shape_layout.copy");

/**
 * alpha.shape_layout.crop
 * Crop operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutCrop = defineStub<TransformOpRequest>("alpha.shape_layout.crop");

/**
 * alpha.shape_layout.deinterleave
 * Deinterleave operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutDeinterleave = defineStub<TransformOpRequest>("alpha.shape_layout.deinterleave");

/**
 * alpha.shape_layout.detach
 * Detach operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutDetach = defineStub<TransformOpRequest>("alpha.shape_layout.detach");

/**
 * alpha.shape_layout.devectorize
 * Devectorize operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutDevectorize = defineStub<TransformOpRequest>("alpha.shape_layout.devectorize");

/**
 * alpha.shape_layout.expand-dims
 * Expand dims operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutExpandDims = defineStub<TransformOpRequest>("alpha.shape_layout.expand-dims");

/**
 * alpha.shape_layout.flatten
 * Flatten operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutFlatten = defineStub<TransformOpRequest>("alpha.shape_layout.flatten");

/**
 * alpha.shape_layout.flip
 * Flip operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutFlip = defineStub<TransformOpRequest>("alpha.shape_layout.flip");

/**
 * alpha.shape_layout.interleave
 * Interleave operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutInterleave = defineStub<TransformOpRequest>("alpha.shape_layout.interleave");

/**
 * alpha.shape_layout.layout-cast
 * Layout cast operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutLayoutCast = defineStub<TransformOpRequest>("alpha.shape_layout.layout-cast");

/**
 * alpha.shape_layout.materialize
 * Materialize operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutMaterialize = defineStub<TransformOpRequest>("alpha.shape_layout.materialize");

/**
 * alpha.shape_layout.memory-format-cast
 * Memory format cast operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutMemoryFormatCast = defineStub<TransformOpRequest>("alpha.shape_layout.memory-format-cast");

/**
 * alpha.shape_layout.move-axis
 * Move axis operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutMoveAxis = defineStub<TransformOpRequest>("alpha.shape_layout.move-axis");

/**
 * alpha.shape_layout.pack
 * Pack operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutPack = defineStub<TransformOpRequest>("alpha.shape_layout.pack");

/**
 * alpha.shape_layout.pad
 * Pad operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutPad = defineStub<TransformOpRequest>("alpha.shape_layout.pad");

/**
 * alpha.shape_layout.permute
 * Permute operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutPermute = defineStub<TransformOpRequest>("alpha.shape_layout.permute");

/**
 * alpha.shape_layout.reinterpret
 * Reinterpret operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutReinterpret = defineStub<TransformOpRequest>("alpha.shape_layout.reinterpret");

/**
 * alpha.shape_layout.repeat
 * Repeat operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutRepeat = defineStub<TransformOpRequest>("alpha.shape_layout.repeat");

/**
 * alpha.shape_layout.repeat-interleave
 * Repeat interleave operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutRepeatInterleave = defineStub<TransformOpRequest>("alpha.shape_layout.repeat-interleave");

/**
 * alpha.shape_layout.replicate
 * Replicate operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutReplicate = defineStub<TransformOpRequest>("alpha.shape_layout.replicate");

/**
 * alpha.shape_layout.reshape
 * Reshape operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutReshape = defineStub<TransformOpRequest>("alpha.shape_layout.reshape");

/**
 * alpha.shape_layout.reshape-like
 * Reshape like operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutReshapeLike = defineStub<TransformOpRequest>("alpha.shape_layout.reshape-like");

/**
 * alpha.shape_layout.reverse
 * Reverse operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutReverse = defineStub<TransformOpRequest>("alpha.shape_layout.reverse");

/**
 * alpha.shape_layout.roll
 * Roll operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutRoll = defineStub<TransformOpRequest>("alpha.shape_layout.roll");

/**
 * alpha.shape_layout.rotate90
 * Rotate90 operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutRotate90 = defineStub<TransformOpRequest>("alpha.shape_layout.rotate90");

/**
 * alpha.shape_layout.shard
 * Shard operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutShard = defineStub<TransformOpRequest>("alpha.shape_layout.shard");

/**
 * alpha.shape_layout.sliding-window-view
 * Sliding window view operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutSlidingWindowView = defineStub<TransformOpRequest>("alpha.shape_layout.sliding-window-view");

/**
 * alpha.shape_layout.squeeze
 * Squeeze operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutSqueeze = defineStub<TransformOpRequest>("alpha.shape_layout.squeeze");

/**
 * alpha.shape_layout.swap-axes
 * Swap axes operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutSwapAxes = defineStub<TransformOpRequest>("alpha.shape_layout.swap-axes");

/**
 * alpha.shape_layout.swizzle
 * Swizzle operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutSwizzle = defineStub<TransformOpRequest>("alpha.shape_layout.swizzle");

/**
 * alpha.shape_layout.tile
 * Tile operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutTile = defineStub<TransformOpRequest>("alpha.shape_layout.tile");

/**
 * alpha.shape_layout.transpose
 * Transpose operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutTranspose = defineStub<TransformOpRequest>("alpha.shape_layout.transpose");

/**
 * alpha.shape_layout.unblock
 * Unblock operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnblock = defineStub<TransformOpRequest>("alpha.shape_layout.unblock");

/**
 * alpha.shape_layout.unflatten
 * Unflatten operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnflatten = defineStub<TransformOpRequest>("alpha.shape_layout.unflatten");

/**
 * alpha.shape_layout.unpack
 * Unpack operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnpack = defineStub<TransformOpRequest>("alpha.shape_layout.unpack");

/**
 * alpha.shape_layout.unreplicate
 * Unreplicate operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnreplicate = defineStub<TransformOpRequest>("alpha.shape_layout.unreplicate");

/**
 * alpha.shape_layout.unshard
 * Unshard operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnshard = defineStub<TransformOpRequest>("alpha.shape_layout.unshard");

/**
 * alpha.shape_layout.unsqueeze
 * Unsqueeze operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnsqueeze = defineStub<TransformOpRequest>("alpha.shape_layout.unsqueeze");

/**
 * alpha.shape_layout.unswizzle
 * Unswizzle operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutUnswizzle = defineStub<TransformOpRequest>("alpha.shape_layout.unswizzle");

/**
 * alpha.shape_layout.vectorize
 * Vectorize operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutVectorize = defineStub<TransformOpRequest>("alpha.shape_layout.vectorize");

/**
 * alpha.shape_layout.view
 * View operation in the shape layout family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const shapeLayoutView = defineStub<TransformOpRequest>("alpha.shape_layout.view");
