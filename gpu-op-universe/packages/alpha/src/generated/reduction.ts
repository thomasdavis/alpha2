/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { ReductionRequest } from "../../../common/src/types";

/**
 * alpha.reduction.all
 * All operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionAll = defineStub<ReductionRequest>("alpha.reduction.all");

/**
 * alpha.reduction.amax
 * Amax operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionAmax = defineStub<ReductionRequest>("alpha.reduction.amax");

/**
 * alpha.reduction.aminmax
 * Aminmax operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionAminmax = defineStub<ReductionRequest>("alpha.reduction.aminmax");

/**
 * alpha.reduction.any
 * Any operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionAny = defineStub<ReductionRequest>("alpha.reduction.any");

/**
 * alpha.reduction.arg-max
 * Arg max operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionArgMax = defineStub<ReductionRequest>("alpha.reduction.arg-max");

/**
 * alpha.reduction.arg-min
 * Arg min operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionArgMin = defineStub<ReductionRequest>("alpha.reduction.arg-min");

/**
 * alpha.reduction.compensated-sum
 * Compensated sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionCompensatedSum = defineStub<ReductionRequest>("alpha.reduction.compensated-sum");

/**
 * alpha.reduction.count-nonzero
 * Count nonzero operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionCountNonzero = defineStub<ReductionRequest>("alpha.reduction.count-nonzero");

/**
 * alpha.reduction.deterministic-reduce
 * Deterministic reduce operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionDeterministicReduce = defineStub<ReductionRequest>("alpha.reduction.deterministic-reduce");

/**
 * alpha.reduction.frobenius-norm
 * Frobenius norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionFrobeniusNorm = defineStub<ReductionRequest>("alpha.reduction.frobenius-norm");

/**
 * alpha.reduction.kahan-sum
 * Kahan sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionKahanSum = defineStub<ReductionRequest>("alpha.reduction.kahan-sum");

/**
 * alpha.reduction.l0-norm
 * L0 norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionL0Norm = defineStub<ReductionRequest>("alpha.reduction.l0-norm");

/**
 * alpha.reduction.l1-norm
 * L1 norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionL1Norm = defineStub<ReductionRequest>("alpha.reduction.l1-norm");

/**
 * alpha.reduction.l2-norm
 * L2 norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionL2Norm = defineStub<ReductionRequest>("alpha.reduction.l2-norm");

/**
 * alpha.reduction.log-sum-exp
 * Log sum exp operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionLogSumExp = defineStub<ReductionRequest>("alpha.reduction.log-sum-exp");

/**
 * alpha.reduction.lp-norm
 * Lp norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionLpNorm = defineStub<ReductionRequest>("alpha.reduction.lp-norm");

/**
 * alpha.reduction.masked-reduce
 * Masked reduce operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMaskedReduce = defineStub<ReductionRequest>("alpha.reduction.masked-reduce");

/**
 * alpha.reduction.max
 * Max operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMax = defineStub<ReductionRequest>("alpha.reduction.max");

/**
 * alpha.reduction.max-with-index
 * Max with index operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMaxWithIndex = defineStub<ReductionRequest>("alpha.reduction.max-with-index");

/**
 * alpha.reduction.mean
 * Mean operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMean = defineStub<ReductionRequest>("alpha.reduction.mean");

/**
 * alpha.reduction.min
 * Min operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMin = defineStub<ReductionRequest>("alpha.reduction.min");

/**
 * alpha.reduction.min-with-index
 * Min with index operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMinWithIndex = defineStub<ReductionRequest>("alpha.reduction.min-with-index");

/**
 * alpha.reduction.moments
 * Moments operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionMoments = defineStub<ReductionRequest>("alpha.reduction.moments");

/**
 * alpha.reduction.neumaier-sum
 * Neumaier sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionNeumaierSum = defineStub<ReductionRequest>("alpha.reduction.neumaier-sum");

/**
 * alpha.reduction.nuclear-norm
 * Nuclear norm operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionNuclearNorm = defineStub<ReductionRequest>("alpha.reduction.nuclear-norm");

/**
 * alpha.reduction.pairwise-sum
 * Pairwise sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionPairwiseSum = defineStub<ReductionRequest>("alpha.reduction.pairwise-sum");

/**
 * alpha.reduction.partial-reduce
 * Partial reduce operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionPartialReduce = defineStub<ReductionRequest>("alpha.reduction.partial-reduce");

/**
 * alpha.reduction.product
 * Product operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionProduct = defineStub<ReductionRequest>("alpha.reduction.product");

/**
 * alpha.reduction.ragged-reduce
 * Ragged reduce operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionRaggedReduce = defineStub<ReductionRequest>("alpha.reduction.ragged-reduce");

/**
 * alpha.reduction.reduce-by-key
 * Reduce by key operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionReduceByKey = defineStub<ReductionRequest>("alpha.reduction.reduce-by-key");

/**
 * alpha.reduction.segmented-max
 * Segmented max operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSegmentedMax = defineStub<ReductionRequest>("alpha.reduction.segmented-max");

/**
 * alpha.reduction.segmented-mean
 * Segmented mean operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSegmentedMean = defineStub<ReductionRequest>("alpha.reduction.segmented-mean");

/**
 * alpha.reduction.segmented-min
 * Segmented min operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSegmentedMin = defineStub<ReductionRequest>("alpha.reduction.segmented-min");

/**
 * alpha.reduction.segmented-sum
 * Segmented sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSegmentedSum = defineStub<ReductionRequest>("alpha.reduction.segmented-sum");

/**
 * alpha.reduction.softmax-statistics
 * Softmax statistics operation in the reduction family.
 * Status: standard; target: architecture-agnostic, future-or-emulated; differentiability: custom-adjoint.
 */
export const reductionSoftmaxStatistics = defineStub<ReductionRequest>("alpha.reduction.softmax-statistics");

/**
 * alpha.reduction.standard-deviation
 * Standard deviation operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionStandardDeviation = defineStub<ReductionRequest>("alpha.reduction.standard-deviation");

/**
 * alpha.reduction.sum
 * Sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSum = defineStub<ReductionRequest>("alpha.reduction.sum");

/**
 * alpha.reduction.sum-abs
 * Sum abs operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSumAbs = defineStub<ReductionRequest>("alpha.reduction.sum-abs");

/**
 * alpha.reduction.sum-squares
 * Sum squares operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSumSquares = defineStub<ReductionRequest>("alpha.reduction.sum-squares");

/**
 * alpha.reduction.superaccumulator-sum
 * Superaccumulator sum operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionSuperaccumulatorSum = defineStub<ReductionRequest>("alpha.reduction.superaccumulator-sum");

/**
 * alpha.reduction.tree-reduce
 * Tree reduce operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionTreeReduce = defineStub<ReductionRequest>("alpha.reduction.tree-reduce");

/**
 * alpha.reduction.variance
 * Variance operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionVariance = defineStub<ReductionRequest>("alpha.reduction.variance");

/**
 * alpha.reduction.welford-moments
 * Welford moments operation in the reduction family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const reductionWelfordMoments = defineStub<ReductionRequest>("alpha.reduction.welford-moments");
