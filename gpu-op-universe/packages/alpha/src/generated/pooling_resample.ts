/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TensorOpRequest } from "../../../common/src/types";

/**
 * alpha.pooling_resample.adaptive-avg-pool
 * Adaptive avg pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleAdaptiveAvgPool = defineStub<TensorOpRequest>("alpha.pooling_resample.adaptive-avg-pool");

/**
 * alpha.pooling_resample.adaptive-max-pool
 * Adaptive max pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleAdaptiveMaxPool = defineStub<TensorOpRequest>("alpha.pooling_resample.adaptive-max-pool");

/**
 * alpha.pooling_resample.area-resize
 * Area resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleAreaResize = defineStub<TensorOpRequest>("alpha.pooling_resample.area-resize");

/**
 * alpha.pooling_resample.avg-pool
 * Avg pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleAvgPool = defineStub<TensorOpRequest>("alpha.pooling_resample.avg-pool");

/**
 * alpha.pooling_resample.bicubic-resize
 * Bicubic resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleBicubicResize = defineStub<TensorOpRequest>("alpha.pooling_resample.bicubic-resize");

/**
 * alpha.pooling_resample.bilinear-resize
 * Bilinear resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleBilinearResize = defineStub<TensorOpRequest>("alpha.pooling_resample.bilinear-resize");

/**
 * alpha.pooling_resample.fractional-max-pool
 * Fractional max pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleFractionalMaxPool = defineStub<TensorOpRequest>("alpha.pooling_resample.fractional-max-pool");

/**
 * alpha.pooling_resample.global-avg-pool
 * Global avg pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleGlobalAvgPool = defineStub<TensorOpRequest>("alpha.pooling_resample.global-avg-pool");

/**
 * alpha.pooling_resample.grid-sample
 * Grid sample operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleGridSample = defineStub<TensorOpRequest>("alpha.pooling_resample.grid-sample");

/**
 * alpha.pooling_resample.lanczos-resize
 * Lanczos resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleLanczosResize = defineStub<TensorOpRequest>("alpha.pooling_resample.lanczos-resize");

/**
 * alpha.pooling_resample.linear-resize
 * Linear resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleLinearResize = defineStub<TensorOpRequest>("alpha.pooling_resample.linear-resize");

/**
 * alpha.pooling_resample.lp-pool
 * Lp pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleLpPool = defineStub<TensorOpRequest>("alpha.pooling_resample.lp-pool");

/**
 * alpha.pooling_resample.max-pool
 * Max pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleMaxPool = defineStub<TensorOpRequest>("alpha.pooling_resample.max-pool");

/**
 * alpha.pooling_resample.min-pool
 * Min pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleMinPool = defineStub<TensorOpRequest>("alpha.pooling_resample.min-pool");

/**
 * alpha.pooling_resample.nearest-resize
 * Nearest resize operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleNearestResize = defineStub<TensorOpRequest>("alpha.pooling_resample.nearest-resize");

/**
 * alpha.pooling_resample.roi-align
 * Roi align operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleRoiAlign = defineStub<TensorOpRequest>("alpha.pooling_resample.roi-align");

/**
 * alpha.pooling_resample.roi-pool
 * Roi pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleRoiPool = defineStub<TensorOpRequest>("alpha.pooling_resample.roi-pool");

/**
 * alpha.pooling_resample.stochastic-pool
 * Stochastic pool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleStochasticPool = defineStub<TensorOpRequest>("alpha.pooling_resample.stochastic-pool");

/**
 * alpha.pooling_resample.unpool
 * Unpool operation in the pooling resample family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const poolingResampleUnpool = defineStub<TensorOpRequest>("alpha.pooling_resample.unpool");
