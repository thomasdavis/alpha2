/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { MatmulRequest } from "../../../common/src/types";

/**
 * alpha.structured_linear.banded-multiply
 * Banded multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearBandedMultiply = defineStub<MatmulRequest>("alpha.structured_linear.banded-multiply");

/**
 * alpha.structured_linear.block-circulant-multiply
 * Block circulant multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearBlockCirculantMultiply = defineStub<MatmulRequest>("alpha.structured_linear.block-circulant-multiply");

/**
 * alpha.structured_linear.block-diagonal-multiply
 * Block diagonal multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearBlockDiagonalMultiply = defineStub<MatmulRequest>("alpha.structured_linear.block-diagonal-multiply");

/**
 * alpha.structured_linear.butterfly-transform
 * Butterfly transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearButterflyTransform = defineStub<MatmulRequest>("alpha.structured_linear.butterfly-transform");

/**
 * alpha.structured_linear.cauchy-multiply
 * Cauchy multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearCauchyMultiply = defineStub<MatmulRequest>("alpha.structured_linear.cauchy-multiply");

/**
 * alpha.structured_linear.cayley-transform
 * Cayley transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearCayleyTransform = defineStub<MatmulRequest>("alpha.structured_linear.cayley-transform");

/**
 * alpha.structured_linear.circulant-multiply
 * Circulant multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearCirculantMultiply = defineStub<MatmulRequest>("alpha.structured_linear.circulant-multiply");

/**
 * alpha.structured_linear.companion-multiply
 * Companion multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearCompanionMultiply = defineStub<MatmulRequest>("alpha.structured_linear.companion-multiply");

/**
 * alpha.structured_linear.diagonal-multiply
 * Diagonal multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearDiagonalMultiply = defineStub<MatmulRequest>("alpha.structured_linear.diagonal-multiply");

/**
 * alpha.structured_linear.fastfood-transform
 * Fastfood transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearFastfoodTransform = defineStub<MatmulRequest>("alpha.structured_linear.fastfood-transform");

/**
 * alpha.structured_linear.givens-sequence
 * Givens sequence operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearGivensSequence = defineStub<MatmulRequest>("alpha.structured_linear.givens-sequence");

/**
 * alpha.structured_linear.givens-transform
 * Givens transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearGivensTransform = defineStub<MatmulRequest>("alpha.structured_linear.givens-transform");

/**
 * alpha.structured_linear.haar-transform
 * Haar transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearHaarTransform = defineStub<MatmulRequest>("alpha.structured_linear.haar-transform");

/**
 * alpha.structured_linear.hadamard-transform
 * Hadamard transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearHadamardTransform = defineStub<MatmulRequest>("alpha.structured_linear.hadamard-transform");

/**
 * alpha.structured_linear.hankel-multiply
 * Hankel multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearHankelMultiply = defineStub<MatmulRequest>("alpha.structured_linear.hankel-multiply");

/**
 * alpha.structured_linear.householder-product
 * Householder product operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearHouseholderProduct = defineStub<MatmulRequest>("alpha.structured_linear.householder-product");

/**
 * alpha.structured_linear.householder-transform
 * Householder transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearHouseholderTransform = defineStub<MatmulRequest>("alpha.structured_linear.householder-transform");

/**
 * alpha.structured_linear.lifting-wavelet-transform
 * Lifting wavelet transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearLiftingWaveletTransform = defineStub<MatmulRequest>("alpha.structured_linear.lifting-wavelet-transform");

/**
 * alpha.structured_linear.low-rank-update
 * Low rank update operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearLowRankUpdate = defineStub<MatmulRequest>("alpha.structured_linear.low-rank-update");

/**
 * alpha.structured_linear.matrix-sign
 * Matrix sign operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearMatrixSign = defineStub<MatmulRequest>("alpha.structured_linear.matrix-sign");

/**
 * alpha.structured_linear.orthogonal-rotation
 * Orthogonal rotation operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearOrthogonalRotation = defineStub<MatmulRequest>("alpha.structured_linear.orthogonal-rotation");

/**
 * alpha.structured_linear.polar-factor
 * Polar factor operation in the structured linear family.
 * Status: research; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearPolarFactor = defineStub<MatmulRequest>("alpha.structured_linear.polar-factor");

/**
 * alpha.structured_linear.randomized-hadamard-transform
 * Randomized hadamard transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearRandomizedHadamardTransform = defineStub<MatmulRequest>("alpha.structured_linear.randomized-hadamard-transform");

/**
 * alpha.structured_linear.sherman-morrison-solve
 * Sherman morrison solve operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearShermanMorrisonSolve = defineStub<MatmulRequest>("alpha.structured_linear.sherman-morrison-solve");

/**
 * alpha.structured_linear.toeplitz-multiply
 * Toeplitz multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearToeplitzMultiply = defineStub<MatmulRequest>("alpha.structured_linear.toeplitz-multiply");

/**
 * alpha.structured_linear.vandermonde-multiply
 * Vandermonde multiply operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearVandermondeMultiply = defineStub<MatmulRequest>("alpha.structured_linear.vandermonde-multiply");

/**
 * alpha.structured_linear.walsh-hadamard-transform
 * Walsh hadamard transform operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearWalshHadamardTransform = defineStub<MatmulRequest>("alpha.structured_linear.walsh-hadamard-transform");

/**
 * alpha.structured_linear.woodbury-solve
 * Woodbury solve operation in the structured linear family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const structuredLinearWoodburySolve = defineStub<MatmulRequest>("alpha.structured_linear.woodbury-solve");
