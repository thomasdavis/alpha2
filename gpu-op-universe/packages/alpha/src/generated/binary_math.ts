/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { ScalarOpRequest } from "../../../common/src/types";

/**
 * alpha.binary_math.add
 * Add operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathAdd = defineStub<ScalarOpRequest>("alpha.binary_math.add");

/**
 * alpha.binary_math.atan2
 * Atan2 operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathAtan2 = defineStub<ScalarOpRequest>("alpha.binary_math.atan2");

/**
 * alpha.binary_math.average-rounded
 * Average rounded operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathAverageRounded = defineStub<ScalarOpRequest>("alpha.binary_math.average-rounded");

/**
 * alpha.binary_math.bitwise-and
 * Bitwise and operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathBitwiseAnd = defineStub<ScalarOpRequest>("alpha.binary_math.bitwise-and");

/**
 * alpha.binary_math.bitwise-or
 * Bitwise or operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathBitwiseOr = defineStub<ScalarOpRequest>("alpha.binary_math.bitwise-or");

/**
 * alpha.binary_math.bitwise-xor
 * Bitwise xor operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathBitwiseXor = defineStub<ScalarOpRequest>("alpha.binary_math.bitwise-xor");

/**
 * alpha.binary_math.complex-div
 * Complex div operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathComplexDiv = defineStub<ScalarOpRequest>("alpha.binary_math.complex-div");

/**
 * alpha.binary_math.complex-mul
 * Complex mul operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathComplexMul = defineStub<ScalarOpRequest>("alpha.binary_math.complex-mul");

/**
 * alpha.binary_math.copy-sign
 * Copy sign operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathCopySign = defineStub<ScalarOpRequest>("alpha.binary_math.copy-sign");

/**
 * alpha.binary_math.div
 * Div operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathDiv = defineStub<ScalarOpRequest>("alpha.binary_math.div");

/**
 * alpha.binary_math.dual-number-mul
 * Dual number mul operation in the binary math family.
 * Status: speculative; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathDualNumberMul = defineStub<ScalarOpRequest>("alpha.binary_math.dual-number-mul");

/**
 * alpha.binary_math.equal
 * Equal operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathEqual = defineStub<ScalarOpRequest>("alpha.binary_math.equal");

/**
 * alpha.binary_math.floor-div
 * Floor div operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathFloorDiv = defineStub<ScalarOpRequest>("alpha.binary_math.floor-div");

/**
 * alpha.binary_math.fmax
 * Fmax operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathFmax = defineStub<ScalarOpRequest>("alpha.binary_math.fmax");

/**
 * alpha.binary_math.fmin
 * Fmin operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathFmin = defineStub<ScalarOpRequest>("alpha.binary_math.fmin");

/**
 * alpha.binary_math.fmod
 * Fmod operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathFmod = defineStub<ScalarOpRequest>("alpha.binary_math.fmod");

/**
 * alpha.binary_math.gcd
 * Gcd operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathGcd = defineStub<ScalarOpRequest>("alpha.binary_math.gcd");

/**
 * alpha.binary_math.greater
 * Greater operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathGreater = defineStub<ScalarOpRequest>("alpha.binary_math.greater");

/**
 * alpha.binary_math.greater-equal
 * Greater equal operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathGreaterEqual = defineStub<ScalarOpRequest>("alpha.binary_math.greater-equal");

/**
 * alpha.binary_math.hypot
 * Hypot operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathHypot = defineStub<ScalarOpRequest>("alpha.binary_math.hypot");

/**
 * alpha.binary_math.interval-add
 * Interval add operation in the binary math family.
 * Status: speculative; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathIntervalAdd = defineStub<ScalarOpRequest>("alpha.binary_math.interval-add");

/**
 * alpha.binary_math.interval-mul
 * Interval mul operation in the binary math family.
 * Status: speculative; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathIntervalMul = defineStub<ScalarOpRequest>("alpha.binary_math.interval-mul");

/**
 * alpha.binary_math.lcm
 * Lcm operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLcm = defineStub<ScalarOpRequest>("alpha.binary_math.lcm");

/**
 * alpha.binary_math.less
 * Less operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLess = defineStub<ScalarOpRequest>("alpha.binary_math.less");

/**
 * alpha.binary_math.less-equal
 * Less equal operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLessEqual = defineStub<ScalarOpRequest>("alpha.binary_math.less-equal");

/**
 * alpha.binary_math.log-add-exp
 * Log add exp operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLogAddExp = defineStub<ScalarOpRequest>("alpha.binary_math.log-add-exp");

/**
 * alpha.binary_math.log-add-exp2
 * Log add exp2 operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLogAddExp2 = defineStub<ScalarOpRequest>("alpha.binary_math.log-add-exp2");

/**
 * alpha.binary_math.logical-and
 * Logical and operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLogicalAnd = defineStub<ScalarOpRequest>("alpha.binary_math.logical-and");

/**
 * alpha.binary_math.logical-or
 * Logical or operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLogicalOr = defineStub<ScalarOpRequest>("alpha.binary_math.logical-or");

/**
 * alpha.binary_math.logical-xor
 * Logical xor operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathLogicalXor = defineStub<ScalarOpRequest>("alpha.binary_math.logical-xor");

/**
 * alpha.binary_math.maximum
 * Maximum operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathMaximum = defineStub<ScalarOpRequest>("alpha.binary_math.maximum");

/**
 * alpha.binary_math.minimum
 * Minimum operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathMinimum = defineStub<ScalarOpRequest>("alpha.binary_math.minimum");

/**
 * alpha.binary_math.mul
 * Mul operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathMul = defineStub<ScalarOpRequest>("alpha.binary_math.mul");

/**
 * alpha.binary_math.next-after
 * Next after operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathNextAfter = defineStub<ScalarOpRequest>("alpha.binary_math.next-after");

/**
 * alpha.binary_math.not-equal
 * Not equal operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathNotEqual = defineStub<ScalarOpRequest>("alpha.binary_math.not-equal");

/**
 * alpha.binary_math.pow
 * Pow operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathPow = defineStub<ScalarOpRequest>("alpha.binary_math.pow");

/**
 * alpha.binary_math.remainder
 * Remainder operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathRemainder = defineStub<ScalarOpRequest>("alpha.binary_math.remainder");

/**
 * alpha.binary_math.rotate-left
 * Rotate left operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathRotateLeft = defineStub<ScalarOpRequest>("alpha.binary_math.rotate-left");

/**
 * alpha.binary_math.rotate-right
 * Rotate right operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathRotateRight = defineStub<ScalarOpRequest>("alpha.binary_math.rotate-right");

/**
 * alpha.binary_math.saturating-add
 * Saturating add operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathSaturatingAdd = defineStub<ScalarOpRequest>("alpha.binary_math.saturating-add");

/**
 * alpha.binary_math.saturating-sub
 * Saturating sub operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathSaturatingSub = defineStub<ScalarOpRequest>("alpha.binary_math.saturating-sub");

/**
 * alpha.binary_math.shift-left
 * Shift left operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathShiftLeft = defineStub<ScalarOpRequest>("alpha.binary_math.shift-left");

/**
 * alpha.binary_math.shift-right-arithmetic
 * Shift right arithmetic operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathShiftRightArithmetic = defineStub<ScalarOpRequest>("alpha.binary_math.shift-right-arithmetic");

/**
 * alpha.binary_math.shift-right-logical
 * Shift right logical operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathShiftRightLogical = defineStub<ScalarOpRequest>("alpha.binary_math.shift-right-logical");

/**
 * alpha.binary_math.squared-difference
 * Squared difference operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathSquaredDifference = defineStub<ScalarOpRequest>("alpha.binary_math.squared-difference");

/**
 * alpha.binary_math.sub
 * Sub operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathSub = defineStub<ScalarOpRequest>("alpha.binary_math.sub");

/**
 * alpha.binary_math.true-div
 * True div operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathTrueDiv = defineStub<ScalarOpRequest>("alpha.binary_math.true-div");

/**
 * alpha.binary_math.trunc-div
 * Trunc div operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathTruncDiv = defineStub<ScalarOpRequest>("alpha.binary_math.trunc-div");

/**
 * alpha.binary_math.unordered-compare
 * Unordered compare operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathUnorderedCompare = defineStub<ScalarOpRequest>("alpha.binary_math.unordered-compare");

/**
 * alpha.binary_math.xlog1py
 * Xlog1py operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathXlog1py = defineStub<ScalarOpRequest>("alpha.binary_math.xlog1py");

/**
 * alpha.binary_math.xlogy
 * Xlogy operation in the binary math family.
 * Status: standard; target: architecture-agnostic; differentiability: usually-differentiable.
 */
export const binaryMathXlogy = defineStub<ScalarOpRequest>("alpha.binary_math.xlogy");
