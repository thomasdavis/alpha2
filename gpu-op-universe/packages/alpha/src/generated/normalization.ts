/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { TensorOpRequest } from "../../../common/src/types";

/**
 * alpha.normalization.batch-norm
 * Batch norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationBatchNorm = defineStub<TensorOpRequest>("alpha.normalization.batch-norm");

/**
 * alpha.normalization.cosine-norm
 * Cosine norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationCosineNorm = defineStub<TensorOpRequest>("alpha.normalization.cosine-norm");

/**
 * alpha.normalization.deep-norm
 * Deep norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationDeepNorm = defineStub<TensorOpRequest>("alpha.normalization.deep-norm");

/**
 * alpha.normalization.fused-add-norm
 * Fused add norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationFusedAddNorm = defineStub<TensorOpRequest>("alpha.normalization.fused-add-norm");

/**
 * alpha.normalization.fused-norm-linear
 * Fused norm linear operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationFusedNormLinear = defineStub<TensorOpRequest>("alpha.normalization.fused-norm-linear");

/**
 * alpha.normalization.group-norm
 * Group norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationGroupNorm = defineStub<TensorOpRequest>("alpha.normalization.group-norm");

/**
 * alpha.normalization.instance-norm
 * Instance norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationInstanceNorm = defineStub<TensorOpRequest>("alpha.normalization.instance-norm");

/**
 * alpha.normalization.layer-norm
 * Layer norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationLayerNorm = defineStub<TensorOpRequest>("alpha.normalization.layer-norm");

/**
 * alpha.normalization.local-response-norm
 * Local response norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationLocalResponseNorm = defineStub<TensorOpRequest>("alpha.normalization.local-response-norm");

/**
 * alpha.normalization.mean-only-norm
 * Mean only norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationMeanOnlyNorm = defineStub<TensorOpRequest>("alpha.normalization.mean-only-norm");

/**
 * alpha.normalization.norm-backward
 * Norm backward operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationNormBackward = defineStub<TensorOpRequest>("alpha.normalization.norm-backward");

/**
 * alpha.normalization.online-norm
 * Online norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationOnlineNorm = defineStub<TensorOpRequest>("alpha.normalization.online-norm");

/**
 * alpha.normalization.power-norm
 * Power norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationPowerNorm = defineStub<TensorOpRequest>("alpha.normalization.power-norm");

/**
 * alpha.normalization.residual-layer-norm
 * Residual layer norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationResidualLayerNorm = defineStub<TensorOpRequest>("alpha.normalization.residual-layer-norm");

/**
 * alpha.normalization.residual-rms-norm
 * Residual rms norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationResidualRmsNorm = defineStub<TensorOpRequest>("alpha.normalization.residual-rms-norm");

/**
 * alpha.normalization.rms-norm
 * Rms norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationRmsNorm = defineStub<TensorOpRequest>("alpha.normalization.rms-norm");

/**
 * alpha.normalization.scale-norm
 * Scale norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationScaleNorm = defineStub<TensorOpRequest>("alpha.normalization.scale-norm");

/**
 * alpha.normalization.spectral-norm
 * Spectral norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationSpectralNorm = defineStub<TensorOpRequest>("alpha.normalization.spectral-norm");

/**
 * alpha.normalization.streaming-norm
 * Streaming norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationStreamingNorm = defineStub<TensorOpRequest>("alpha.normalization.streaming-norm");

/**
 * alpha.normalization.unit-norm
 * Unit norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationUnitNorm = defineStub<TensorOpRequest>("alpha.normalization.unit-norm");

/**
 * alpha.normalization.weight-norm
 * Weight norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationWeightNorm = defineStub<TensorOpRequest>("alpha.normalization.weight-norm");

/**
 * alpha.normalization.welford-norm
 * Welford norm operation in the normalization family.
 * Status: standard; target: architecture-agnostic; differentiability: differentiable.
 */
export const normalizationWelfordNorm = defineStub<TensorOpRequest>("alpha.normalization.welford-norm");
