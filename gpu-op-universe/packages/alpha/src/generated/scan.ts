/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { ScanRequest } from "../../../common/src/types";

/**
 * alpha.scan.affine-recurrence-scan
 * Affine recurrence scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanAffineRecurrenceScan = defineStub<ScanRequest>("alpha.scan.affine-recurrence-scan");

/**
 * alpha.scan.associative-scan
 * Associative scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanAssociativeScan = defineStub<ScanRequest>("alpha.scan.associative-scan");

/**
 * alpha.scan.bidirectional-scan
 * Bidirectional scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanBidirectionalScan = defineStub<ScanRequest>("alpha.scan.bidirectional-scan");

/**
 * alpha.scan.blelloch-scan
 * Blelloch scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanBlellochScan = defineStub<ScanRequest>("alpha.scan.blelloch-scan");

/**
 * alpha.scan.decoupled-lookback-scan
 * Decoupled lookback scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanDecoupledLookbackScan = defineStub<ScanRequest>("alpha.scan.decoupled-lookback-scan");

/**
 * alpha.scan.delta-rule-scan
 * Delta rule scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanDeltaRuleScan = defineStub<ScanRequest>("alpha.scan.delta-rule-scan");

/**
 * alpha.scan.exclusive-scan
 * Exclusive scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanExclusiveScan = defineStub<ScanRequest>("alpha.scan.exclusive-scan");

/**
 * alpha.scan.gated-delta-rule-scan
 * Gated delta rule scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanGatedDeltaRuleScan = defineStub<ScanRequest>("alpha.scan.gated-delta-rule-scan");

/**
 * alpha.scan.hillis-steele-scan
 * Hillis steele scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanHillisSteeleScan = defineStub<ScanRequest>("alpha.scan.hillis-steele-scan");

/**
 * alpha.scan.inclusive-scan
 * Inclusive scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanInclusiveScan = defineStub<ScanRequest>("alpha.scan.inclusive-scan");

/**
 * alpha.scan.linear-recurrence-scan
 * Linear recurrence scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanLinearRecurrenceScan = defineStub<ScanRequest>("alpha.scan.linear-recurrence-scan");

/**
 * alpha.scan.matrix-recurrence-scan
 * Matrix recurrence scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanMatrixRecurrenceScan = defineStub<ScanRequest>("alpha.scan.matrix-recurrence-scan");

/**
 * alpha.scan.parallel-prefix-doubling
 * Parallel prefix doubling operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanParallelPrefixDoubling = defineStub<ScanRequest>("alpha.scan.parallel-prefix-doubling");

/**
 * alpha.scan.prefix-and
 * Prefix and operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixAnd = defineStub<ScanRequest>("alpha.scan.prefix-and");

/**
 * alpha.scan.prefix-log-sum-exp
 * Prefix log sum exp operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixLogSumExp = defineStub<ScanRequest>("alpha.scan.prefix-log-sum-exp");

/**
 * alpha.scan.prefix-max
 * Prefix max operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixMax = defineStub<ScanRequest>("alpha.scan.prefix-max");

/**
 * alpha.scan.prefix-min
 * Prefix min operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixMin = defineStub<ScanRequest>("alpha.scan.prefix-min");

/**
 * alpha.scan.prefix-or
 * Prefix or operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixOr = defineStub<ScanRequest>("alpha.scan.prefix-or");

/**
 * alpha.scan.prefix-product
 * Prefix product operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixProduct = defineStub<ScanRequest>("alpha.scan.prefix-product");

/**
 * alpha.scan.prefix-sum
 * Prefix sum operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixSum = defineStub<ScanRequest>("alpha.scan.prefix-sum");

/**
 * alpha.scan.prefix-xor
 * Prefix xor operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanPrefixXor = defineStub<ScanRequest>("alpha.scan.prefix-xor");

/**
 * alpha.scan.reverse-scan
 * Reverse scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanReverseScan = defineStub<ScanRequest>("alpha.scan.reverse-scan");

/**
 * alpha.scan.scan-by-key
 * Scan by key operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanScanByKey = defineStub<ScanRequest>("alpha.scan.scan-by-key");

/**
 * alpha.scan.segmented-scan
 * Segmented scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanSegmentedScan = defineStub<ScanRequest>("alpha.scan.segmented-scan");

/**
 * alpha.scan.selective-scan
 * Selective scan operation in the scan family.
 * Status: standard; target: architecture-agnostic; differentiability: custom-adjoint.
 */
export const scanSelectiveScan = defineStub<ScanRequest>("alpha.scan.selective-scan");
