/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { MemoryOpRequest } from "../../../common/src/types";

/**
 * gaia.arena_pool.arena-checkpoint
 * Arena checkpoint operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaCheckpoint = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-checkpoint");

/**
 * gaia.arena_pool.arena-fragmentation-report
 * Arena fragmentation report operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaFragmentationReport = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-fragmentation-report");

/**
 * gaia.arena_pool.arena-guard
 * Arena guard operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaGuard = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-guard");

/**
 * gaia.arena_pool.arena-leak-report
 * Arena leak report operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaLeakReport = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-leak-report");

/**
 * gaia.arena_pool.arena-poison
 * Arena poison operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaPoison = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-poison");

/**
 * gaia.arena_pool.arena-restore
 * Arena restore operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaRestore = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-restore");

/**
 * gaia.arena_pool.arena-rollback
 * Arena rollback operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaRollback = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-rollback");

/**
 * gaia.arena_pool.arena-snapshot
 * Arena snapshot operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaSnapshot = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-snapshot");

/**
 * gaia.arena_pool.arena-validate
 * Arena validate operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolArenaValidate = defineStub<MemoryOpRequest>("gaia.arena_pool.arena-validate");

/**
 * gaia.arena_pool.create-arena
 * Create arena operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolCreateArena = defineStub<MemoryOpRequest>("gaia.arena_pool.create-arena");

/**
 * gaia.arena_pool.create-pool
 * Create pool operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolCreatePool = defineStub<MemoryOpRequest>("gaia.arena_pool.create-pool");

/**
 * gaia.arena_pool.destroy-arena
 * Destroy arena operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolDestroyArena = defineStub<MemoryOpRequest>("gaia.arena_pool.destroy-arena");

/**
 * gaia.arena_pool.destroy-pool
 * Destroy pool operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolDestroyPool = defineStub<MemoryOpRequest>("gaia.arena_pool.destroy-pool");

/**
 * gaia.arena_pool.pool-acquire
 * Pool acquire operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolPoolAcquire = defineStub<MemoryOpRequest>("gaia.arena_pool.pool-acquire");

/**
 * gaia.arena_pool.pool-defragment
 * Pool defragment operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolPoolDefragment = defineStub<MemoryOpRequest>("gaia.arena_pool.pool-defragment");

/**
 * gaia.arena_pool.pool-release
 * Pool release operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolPoolRelease = defineStub<MemoryOpRequest>("gaia.arena_pool.pool-release");

/**
 * gaia.arena_pool.pool-trim
 * Pool trim operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolPoolTrim = defineStub<MemoryOpRequest>("gaia.arena_pool.pool-trim");

/**
 * gaia.arena_pool.suballocate
 * Suballocate operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolSuballocate = defineStub<MemoryOpRequest>("gaia.arena_pool.suballocate");

/**
 * gaia.arena_pool.subfree
 * Subfree operation in the arena pool family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const arenaPoolSubfree = defineStub<MemoryOpRequest>("gaia.arena_pool.subfree");
