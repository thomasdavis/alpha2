/* AUTO-GENERATED. Do not hand-edit; edit operation-registry.json. */
import { defineStub } from "../../../common/src/types";
import type { MemoryOpRequest } from "../../../common/src/types";

/**
 * gaia.allocation.allocate-code-memory
 * Allocate code memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateCodeMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-code-memory");

/**
 * gaia.allocation.allocate-coherent-memory
 * Allocate coherent memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateCoherentMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-coherent-memory");

/**
 * gaia.allocation.allocate-constant-memory
 * Allocate constant memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateConstantMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-constant-memory");

/**
 * gaia.allocation.allocate-contiguous-memory
 * Allocate contiguous memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateContiguousMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-contiguous-memory");

/**
 * gaia.allocation.allocate-pinned-host-memory
 * Allocate pinned host memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocatePinnedHostMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-pinned-host-memory");

/**
 * gaia.allocation.allocate-protected-memory
 * Allocate protected memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateProtectedMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-protected-memory");

/**
 * gaia.allocation.allocate-queue-memory
 * Allocate queue memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateQueueMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-queue-memory");

/**
 * gaia.allocation.allocate-scratch-memory
 * Allocate scratch memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateScratchMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-scratch-memory");

/**
 * gaia.allocation.allocate-semaphore-memory
 * Allocate semaphore memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateSemaphoreMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-semaphore-memory");

/**
 * gaia.allocation.allocate-sparse-memory
 * Allocate sparse memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateSparseMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-sparse-memory");

/**
 * gaia.allocation.allocate-state-memory
 * Allocate state memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateStateMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-state-memory");

/**
 * gaia.allocation.allocate-system-memory
 * Allocate system memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateSystemMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-system-memory");

/**
 * gaia.allocation.allocate-video-memory
 * Allocate video memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateVideoMemory = defineStub<MemoryOpRequest>("gaia.allocation.allocate-video-memory");

/**
 * gaia.allocation.allocate-workspace
 * Allocate workspace operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationAllocateWorkspace = defineStub<MemoryOpRequest>("gaia.allocation.allocate-workspace");

/**
 * gaia.allocation.free-memory
 * Free memory operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationFreeMemory = defineStub<MemoryOpRequest>("gaia.allocation.free-memory");

/**
 * gaia.allocation.query-allocation
 * Query allocation operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationQueryAllocation = defineStub<MemoryOpRequest>("gaia.allocation.query-allocation");

/**
 * gaia.allocation.set-allocation-compressibility
 * Set allocation compressibility operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationSetAllocationCompressibility = defineStub<MemoryOpRequest>("gaia.allocation.set-allocation-compressibility");

/**
 * gaia.allocation.set-allocation-priority
 * Set allocation priority operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationSetAllocationPriority = defineStub<MemoryOpRequest>("gaia.allocation.set-allocation-priority");

/**
 * gaia.allocation.set-allocation-tag
 * Set allocation tag operation in the allocation family.
 * Status: standard; target: host; differentiability: not-applicable.
 */
export const allocationSetAllocationTag = defineStub<MemoryOpRequest>("gaia.allocation.set-allocation-tag");
