{
  "name": "@alpha/server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@alpha/core": "*",
    "@alpha/tensor": "*",
    "@alpha/autograd": "*",
    "@alpha/tokenizers": "*",
    "@alpha/model": "*",
    "@alpha/train": "*",
    "@alpha/db": "*",
    "ai": "^6.0.0",
    "@ai-sdk/provider": "^3.0.0",
    "@ai-sdk/provider-utils": "^4.0.0",
    "effect": "^3.12.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
